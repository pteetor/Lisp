//
// Global functions
//

extern int length(Cell* list);
extern Cell* elide(Cell* list, Cell* elem);

extern Cell* makeList(Cell*);
extern Cell* makeList(Cell*, Cell*);
extern Cell* makeList(Cell*, Cell*, Cell*);
